spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/test
    username: sa
    password:
    driver-class-name: org.h2.Driver
    hikari:
      maximum-pool-size: 30

  jpa:
#        show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: none
    generate-ddl: true
    open-in-view: false

#logging:
#  slack:
#    webhook-uri: ${AWS_SM_SLACK_WEBHOOK_URL}
#  config: classpath:logback-slack.xml
#  level:
#    org:
#      hibernate:
#        SQL: debug
#        type:
#          descriptor:
#            sql: trace
#    com:
#      amazonaws:
#        util:
#          EC2MetadataUtils: error
#
#jwt:
#  secret: ${JWT_SECRET}

cloud:
  aws:
    credentials:
      use-default-aws-credentials-chain: true
      accessKey: AKIAS5MSEJDE2ZZPTW6G
      secretKey: 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
    s3:
      bucket: worry-record
    region:
      static: ap-northeast-2
    stack:
      auto: false